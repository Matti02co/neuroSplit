# neuroSplit
Chrome side-panel extension that splits long text into word-sized chunks you can copy into an LLM one at a time. For when a big paste gets turned into a file or drag and drop does not work..
